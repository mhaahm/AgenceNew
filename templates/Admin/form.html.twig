{{ form_start(formProperty) }}
<div class="row">
    <div class="col-md-4">{{ form_row(formProperty.title) }}</div>
    <div class="col-md-4">{{ form_row(formProperty.price) }}</div>
    <div class="col-md-4">{{ form_row(formProperty.surface) }}</div>
</div>
<div class="row">
    <div class="col-md-4">{{ form_row(formProperty.rooms) }}</div>
    <div class="col-md-4">{{ form_row(formProperty.bedrooms) }}</div>
    <div class="col-md-4">{{ form_row(formProperty.floor) }}</div>

</div>

<div class="row">
    <div class="col-md-3">{{ form_row(formProperty.heat) }}</div>
    <div class="col-md-3">{{ form_row(formProperty.city) }}</div>
    <div class="col-md-3">{{ form_row(formProperty.adress) }}</div>
    <div class="col-md-3">{{ form_row(formProperty.postal_code) }}</div>
</div>
<div class="row">
    <div class="col-md-4">{{ form_row(formProperty.description) }}</div>
    <div class="col-md-4">{{ form_row(formProperty.options) }}</div>
    <div class="col-md-4">{{ form_row(formProperty.imageFile) }}</div>
</div>
<div class="row">
    <div class="col-md-2">{{ form_row(formProperty.sold) }}</div>
</div>
<button class="btn btn-success">{{ bouton }}</button>
{{ form_rest(formProperty) }}