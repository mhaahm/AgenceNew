{% extends 'base.html.twig' %}
{% block body %}
    <div class="col-lg-12">
        <h1 class="page-header">Agence Lorem ipsume</h1>

    </div>

        <div class="container">
            {{ form_start(form) }}
            <div class="form-row align-items-end">
                <div class="col" style="display: inline-block">
                    {{ form_row(form.max_price) }}
                </div>
                <div class="col" style="display: inline-block">
                    {{ form_row(form.min_surface) }}
                </div>
                <div class="col" style="display: inline-block">
                    {{ form_row(form.options) }}
                </div>
                <div class="col" style="display: inline-block">
                    <button type="submit" class="btn btn-success">Rechercher</button>
                    {{ properties |length }} Biens
                </div>
            </div>
            {{ form_end(form) }}
        </div>

    <div class="col-lg-12">
        <div class="container">
            <div style="display: grid; grid-template-columns: repeat(4,1fr);grid-column-gap: 20px;">
                {% for propertie in properties %}
                    {%  include('property/_property.html.twig') %}
                {% endfor %}

            </div>
        </div>
    </div>
{% endblock %}